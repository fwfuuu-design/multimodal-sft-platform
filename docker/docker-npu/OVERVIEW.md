# Ascend NPU container

The NPU image retains the CANN, PyTorch/TorchNPU, and Triton Ascend dependencies required by the Multimodal SFT Platform hardware adapter.

Build from the project root with the local source as context. Production deployments must inject credentials and controlled storage paths; model weights are not included.
